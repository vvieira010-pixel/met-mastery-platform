# 60-second speaking tasks

This folder contains the speaking tasks from the 29 prompt sets whose source data specifies `60` seconds.

Current count: 58 prompt-file tasks, plus 15 image-backed `Describe the Picture` tasks (73 total).
